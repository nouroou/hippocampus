import 'package:flutter/material.dart';
import 'package:hippocampus/models/user.dart';

class SearchPage extends StatelessWidget {
  User userProvider;
  SearchPage({super.key, required this.userProvider});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
